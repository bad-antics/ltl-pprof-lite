// examples/demo.ltl — runnable demo for ltl_pprof_lite

import std::io
use ltl_pprof_lite as lib

fn main() {
    io::println("ltl_pprof_lite demo starting...")
    let result = lib::main() |> io::inspect()
    io::println("demo complete")
}